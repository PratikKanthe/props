import React from "react";

export default function FuncProps(props){
    return(
        <div>
            <h3> Hello {props.name}, from {props.place} Welcome! to Functional Props </h3>
        </div>
    );
}